<!-- 
GLTF to JSX: https://gltf.pmnd.rs/

! Problems:
Yo! To remove a file, you use:

```bash
rm filename
```

Here are some more useful commands for a full-stack developer:

1. **Navigate Directories:**
   - `cd foldername` - Change directory.
   - `cd ..` - Go up one directory.

2. **List Files:**
   - `ls` - List files in the current directory.
   - `ls -la` - List all files, including hidden ones, with details.

3. **Copy Files:**
   - `cp sourcefile destination` - Copy a file.
   - `cp -r sourcedir destinationdir` - Copy a directory recursively.

4. **Move/Rename Files:**
   - `mv oldfilename newfilename` - Rename or move a file.

5. **View File Contents:**
   - `cat filename` - Display the contents of a file.
   - `less filename` - View file contents with scrolling.

6. **Search for Text in Files:**
   - `grep 'search_term' filename` - Search for a term in a file.
   - `grep -r 'search_term' foldername` - Search recursively in a folder.

7. **Environment Variables:**
   - `export VAR_NAME=value` - Set an environment variable.
   - `echo $VAR_NAME` - Display the value of an environment variable.

8. **Version Control with Git:**
   - `git status` - Check the status of your git repository.
   - `git add .` - Stage all changes.
   - `git commit -m "your message"` - Commit changes with a message.
   - `git push` - Push changes to the remote repository.
   - `git pull` - Fetch and merge changes from the remote repository.

9. **Node.js:**
   - `npm install package_name` - Install a package.
   - `npm start` - Start your Node.js application.
   - `npm run build` - Build your application for production.

10. **Database Commands (if using MongoDB):**
    - `mongo` - Start the MongoDB shell.
    - `show dbs` - Show databases.
    - `use dbname` - Switch to a specific database. 
 -->